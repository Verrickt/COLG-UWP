<local:MenuPage
    x:Class="Colg_UWP.View.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Colg_UWP.View.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:model="using:Colg_UWP.Model"
    xmlns:controls="using:Colg_UWP.View.Controls"
   
    >
    <Page.Resources>
        <DataTemplate x:Key="ArticleTemplate" x:DataType="model:Article" >
            <controls:ArticleTemplate></controls:ArticleTemplate>
        </DataTemplate>
    </Page.Resources>
    <Pivot Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <PivotItem>
            <PivotItem.Header>
                <TextBlock Style="{StaticResource BodyTextBlockStyle }" Margin="12,0,0,0" >首页</TextBlock>
            </PivotItem.Header>
                <GridView SizeChanged="GridView_SizeChanged" ScrollViewer.VerticalScrollBarVisibility="Hidden" ItemTemplate="{StaticResource ArticleTemplate}" ItemsSource="{x:Bind VM.News,Mode=OneWay}" ItemClick="GridView_ItemClick" IsItemClickEnabled="True" SelectionMode="None">
                <GridView.ItemContainerStyle>
                    <Style TargetType="GridViewItem">
                        <Setter Property="VerticalContentAlignment" Value="Stretch"></Setter>
                        <Setter Property="Margin" Value="10"></Setter>
                    </Style>
                </GridView.ItemContainerStyle>

            </GridView>
        </PivotItem>
        <PivotItem>
            <PivotItem.Header>
                <TextBlock Style="{StaticResource BodyTextBlockStyle }"> 资讯</TextBlock>
            </PivotItem.Header>
            <GridView Name="HotNews" ItemClick="GridView_ItemClick" SizeChanged="GridView_SizeChanged" ScrollViewer.VerticalScrollBarVisibility="Hidden" ItemTemplate="{StaticResource ArticleTemplate}" ItemsSource="{x:Bind VM.HotNews,Mode=OneWay}" IsItemClickEnabled="True" SelectionMode="None">

                <GridView.ItemContainerStyle>
                    <Style TargetType="GridViewItem">
                        <Setter Property="VerticalContentAlignment" Value="Stretch"></Setter>
                        <Setter Property="Margin" Value="10"></Setter>
                    </Style>
                </GridView.ItemContainerStyle>

            </GridView>
        </PivotItem>
        <PivotItem>
            <PivotItem.Header>
                <TextBlock Style="{StaticResource BodyTextBlockStyle }">前瞻</TextBlock>
            </PivotItem.Header>
            <GridView Name="Prospective" IsItemClickEnabled="True" ItemClick="GridView_ItemClick" SizeChanged="GridView_SizeChanged" ScrollViewer.VerticalScrollBarVisibility="Hidden" ItemTemplate="{StaticResource ArticleTemplate}" ItemsSource="{x:Bind VM.Prospective,Mode=OneWay}">

                <GridView.ItemContainerStyle>
                <Style TargetType="GridViewItem">
                    <Setter Property="VerticalContentAlignment" Value="Stretch"></Setter>
                    <Setter Property="Margin" Value="10"></Setter>
                </Style>
                </GridView.ItemContainerStyle>
            </GridView>
        </PivotItem>
    </Pivot>
    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Name="Refresh" Icon="Refresh" Label="刷新" Click="Refresh_Click"></AppBarButton>
        </CommandBar>
    </Page.BottomAppBar>
</local:MenuPage>



