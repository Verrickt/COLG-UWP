<local:MenuPage
    x:Class="Colg_UWP.View.Pages.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Colg_UWP.View.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodel="using:Colg_UWP.ViewModel"
    xmlns:model="using:Colg_UWP.Model"
    xmlns:controls="using:Colg_UWP.View.Controls"
    Loaded="LoginPage_OnLoaded"
    mc:Ignorable="d"
    >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <CommandBar Grid.Row="0">
            <AppBarButton Icon="Refresh" Label="刷新"></AppBarButton>
        </CommandBar>
        <RelativePanel Grid.Row="1" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <StackPanel  RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.AlignVerticalCenterWithPanel="True">
                <ListView Name="SavedLoginDatas" ItemsSource="{x:Bind VM.SavedLogins,Mode=OneWay}" 
                  SelectionChanged="SavedLoginDatas_OnSelectionChanged"  >

                </ListView>
                <StackPanel>
                    <TextBox Text="{x:Bind VM.CurrentLogin.Username,Mode=TwoWay}"  Margin="10,5" HorizontalAlignment="Stretch" Name="UserName" PlaceholderText="用户名" >
                    </TextBox>
                    <PasswordBox Password="{x:Bind VM.CurrentLogin.Password,Mode=TwoWay}" Margin="10,5"  HorizontalAlignment="Stretch" Name="Password" PlaceholderText="密码" PasswordRevealMode="Peek" ></PasswordBox>
                    <ComboBox Name="SecurityQuestionComboBox" Margin="10,5" 
                          HorizontalAlignment="Stretch" 
                          SelectedIndex="{x:Bind VM.CurrentLogin.QuestionID,Mode=TwoWay}"
                          ItemsSource="{x:Bind VM.SecurityQuestions}"   
                          PlaceholderText="安全提问(未设置请忽略)"></ComboBox>
                    <TextBox  Margin="10,5"  Text="{x:Bind VM.CurrentLogin.Answer,Mode=TwoWay}"  PlaceholderText="问题答案"></TextBox>
                    <StackPanel  Margin="10,5" Orientation="Horizontal">
                        <Button Content="登陆" Name="LoginButton" Click="LoginButton_OnClick" ></Button>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </RelativePanel>
    </Grid>
</local:MenuPage>
