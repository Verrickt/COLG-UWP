<Page
    x:Class="Colg_UWP.View.Pages.NewReplyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Colg_UWP.View.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" >
            <TextBlock Text="{x:Bind VM.Discussion.Subject}"></TextBlock>
        </StackPanel>
<TextBox Name="Reply" Text="{x:Bind VM.ReplyMessage,Mode=TwoWay}"
         TextWrapping="Wrap"
        AcceptsReturn="True"
         Grid.Row="1" >
</TextBox>

        <CommandBar Grid.Row="2" ClosedDisplayMode="Compact" HorizontalAlignment="Right"
                   
                    >
            <AppBarButton  Icon="Send" Label="发送" Name="Post" Click="Post_OnClick" ></AppBarButton>
        </CommandBar>
    </Grid>
   
</Page>
